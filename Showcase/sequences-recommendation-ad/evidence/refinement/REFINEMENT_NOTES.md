# Refinement record

The first strict passing version was recorded before these refinement rounds with `check --json --strict --frame-check`: 0 errors and 0 warnings across lint, runtime, layout, motion, and contrast.

## Round 1 - ChatGPT knot animation

- Added the original 25-frame local knot-unfurl sprite from the existing ChatGPT showcase.
- Inverted its white-on-transparent pixels for the light composer field.
- Swapped the final sprite frame atomically to the canonical SVG and added one restrained completion pulse.
- Confirmed every sprite phase and vector handoff in full-frame and 4x close-up snapshots under `round-1-logo-*`.

## Round 2 - final promise handoff

- Made the end composition opaque from its first rendered frame to eliminate the product/end seam.
- Advanced the two promise lines, result tile, proof, and index so the claim begins resolving within two-tenths of the cut.
- Preserved the long readable hold and subtle lockup breath.
- Inspected the cut at 0.1-second intervals plus a 2x lockup crop under `round-2-final-*`.

## Round 3 - storyboard-to-player proof

- Added an explicit `STORYBOARD LOCKED / 05 BEATS` completion state.
- Added one selected-card pulse before the measured card-to-player expansion.
- Verified the complete story strip, morph, source prompt, conventional advice, recommendation, and verified result in full-frame and 2x product crops under `round-3-product-*`.

No visual defect remained after round 3: type is contained, every primary state is readable, transition custody is opaque, and the composition preserves safe margins.

## Directed polish - ChatGPT hijack

The answer beat received a user-directed hijack treatment: three discrete horizontal signal tears, a bounded answer-block jitter, and a single `SEQUENCES OVERRIDE / SIGNAL ACQUIRED` diagnostic over 0.42 seconds. Targeted full-frame and 2x snapshots under `user-glitch-*` proved the effect before the all-transition gate.

## Directed revision - ChatGPT-native design and music sync

- Replaced the cream/cobalt poster language with neutral ChatGPT-like fields, white conversational surfaces, dark ink, rounded controls, and restrained OpenAI green.
- Carried the animated knot from the opening into the product header, player, and final lockup.
- Replaced the previous music bed with the exact `happy_comercial.mp3` catalog master.
- Snapped the complete edit to its 60.1 BPM onset grid: the question, send, hijack, recommendation, product cut, Build press, storyboard lock, card-to-player morph, verified state, and final promise all land on recorded 30fps beat frames.
- Inspected 21 exact-beat frames and three contact sheets under `user-openai-sync-pass/`, followed by an encoded MP4 contact sheet and four full-resolution QA frames.

## Directed revision - real ChatGPT response

- Removed the detached advice checklist and production-pressure side card.
- Kept the submitted question as a user bubble and introduced one canonical-knot assistant avatar, `ChatGPT` identity, and a single coherent streaming response.
- Interrupted the live response in place with the existing signal fault, preserving the 5.533-second music/SFX hit and every downstream beat.
- Inspected two targeted chat passes plus the encoded 5.300-second full-resolution frame before rerendering the master.

## Directed revision - two-turn refusal and takeover

- Extended the film from 18.6 to 23.6 seconds while preserving the seven-second Sequences product proof and three-second final hold.
- Replaced the first response's coarse text states with deterministic character-by-character typing.
- Scrolled the real conversation, typed and submitted `Can you make a launch film`, then typed `No, as an AI agent, I cannot directly help you—` until the 10.533-second hijack cut it off.
- Added typing SFX to all four visible typing windows and click SFX to both ChatGPT sends while keeping the requested `happy_comercial` bed on the 30fps beat grid.
- Fixed preserved-whitespace in the refusal block so the canonical avatar, `ChatGPT` label, and response share one top baseline.
- Inspected focused source frames under `user-two-turn-pass/`, `user-two-turn-glitch/`, and `user-two-turn-alignment/`, then inspected encoded refusal and glitch frames from the final MP4.
