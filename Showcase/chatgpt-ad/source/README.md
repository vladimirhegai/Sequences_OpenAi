# ChatGPT ad source

Current HyperFrames showcase project. `index.html` is the host composition;
`compositions/` contains the composer, work-world, and end-lockup sub-compositions.
All visual dependencies are local under `assets/`.

Run HyperFrames commands against this `source/` directory. See `AUTHORING.md` for the
beat map, motion lessons, render command, and exact audio mix.
