# QA

The complete HyperFrames gate passed with 0 errors and 0 warnings on 2026-07-19:

```powershell
bun scripts/hyperframes.ts check Showcase/chatgpt-ad/source --json --strict --snapshots --at-transitions --frame-check
```

The final render is 27.600 seconds, 1920×1080 at 30fps. Its audio stream is AAC,
48kHz, stereo.
